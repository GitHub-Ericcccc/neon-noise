# neon-noise：Spectroid 风格开发修改方案

- 编制日期：2026-09-11
- 状态：待开发；本文件仅保存方案，不代表代码已修改或已验证
- 原始上游仓库：https://github.com/Arrow36/neon-noise
- 实际开发仓库：https://github.com/GitHub-Ericcccc/neon-noise
- 本地开发目录：`D:\all_z\Documents\GitHub\neon-noise`
- 计划开发分支：`modify-for-spectroid`，后续开发时创建或核实后使用
- 许可决策：继续采用 MIT，保留上游版权及完整许可声明
- 历史代码分析基线：`0f96ec683b72cf1f4b2dde26d24ec9ce3ee2fefc`
- 本次本地核查提交：`b38c4f9d7e25a8359fb50f81ce72ab50e3295d4d`；当前分支为 `main`
- 修订说明：补充 fork 开发、MIT 与来源说明要求；本次仅更新方案，未创建分支或实施功能修改
- 基线文件：https://github.com/Arrow36/neon-noise/blob/0f96ec683b72cf1f4b2dde26d24ec9ce3ee2fefc/index.html
- 工作范围：在现有单文件应用内实现接近用户提供的 Spectroid 截图的呈现，保留现有历史分析功能。

## 一、目标与事实边界

### 1. 已从源码确认

历史分析基线仓库包含 `index.html`、`README.md`、`LICENSE`。该历史基线的应用页面、CSS、JavaScript 均位于 2311 行的 `index.html`，使用浏览器 Web Audio API、Canvas 和 SVG，无后端及外部前端依赖。以下函数和行为分析对应历史基线，不能直接视为当前 fork 的逐项核验结果。

2026-09-11 本地核查：当前提交为 `b38c4f9d7e25a8359fb50f81ce72ab50e3295d4d`，本地对象库不能解析上述 `0f96ec6...` 提交，直接比较返回 `bad object`。这不足以判断两者的祖先关系或差异原因。开发前应核对 fork 当前源码，取得并验证上游基准后再做比较；不得把历史分析 SHA 直接写成当前开发分支的父提交。

- 实时数据：`spectrum`。
- 第二条曲线：`peakHold`，当前以每秒 4.5 dB 衰减。
- FFT：单个 `AnalyserNode`，`fftSize = 32768`。
- 历史：`historyFrames` 保存频谱快照及采集时间戳，按 20 秒裁剪。
- 瀑布图：`waterfallBuffer` 保存图像，约每 110 ms 下移 2 像素并在顶部加入新行。
- 历史分析：平均功率、中位数、最大响声、噪声突出度与出现率。
- 当前“暂停采集”仅停止应用更新，不停止麦克风轨道或暂停 AudioContext。
- 当前从历史分析“返回实时频谱”不会自动恢复采集。

### 2. 截图可见事实

用户提供的照片中，上方为深色频谱图，包含黄色和红色曲线，以及青色、绿色频率标记；下方为彩色历史瀑布图。图表占据主要屏幕空间。下方左侧的负数刻度标有 dB，不应直接解释为历史秒数。

截图原始位置（临时文件，可能失效）：`C:\Users\all_z\AppData\Local\Temp\codex-clipboard-24b97fff-7229-4eb6-a41b-351b520baec2.png`。本方案未归档该图片；未来要求精确视觉复刻时应确认参考图仍可用。

### 3. 待核实，不能作为已证实事实

- Spectroid 黄线通常理解为当前频谱、红线为最大值保持，但本轮未取得可核验的官方颜色定义。开发图例采用本项目自行明确的语义。
- Spectroid 红线的保持时长、衰减策略、重置条件及截图对应设置尚未核实。
- 下图 dB 标尺的具体交互和显示规则需进一步核实，不能仅凭静态照片完全还原。
- 官方商店说明采用多个频率范围重叠的 FFT，并支持双指缩放；具体算法实现尚未审阅。
- 当前结论来自静态代码阅读，尚未做真实麦克风及跨设备对照测试。

官方说明：https://play.google.com/store/apps/details?id=org.intoorbit.spectrum

## 二、首阶段开发范围

首阶段实现“Spectroid 风格显示”，继续使用当前音频分析器、纯前端架构及单文件交付。以清晰的双曲线和滚动历史为核心，不承诺与 Spectroid 算法、数值或每个像素完全一致。

1. 紧凑工具栏，减少大标题、装饰背景及独立数值卡片占用。
2. 黄色实时频谱、红色峰值保持；图例明确语义。
3. 深色背景、细线曲线、主次网格，减少发光与面积填充。
4. 上下图共用频率坐标，明确电平轴、频率轴、时间方向和颜色标尺。
5. 蓝紫为主的瀑布图色阶，并提供独立的颜色电平范围。
6. 保留实时采集、增益、暂停/继续、自定义测量点、清空历史及四类历史分析。
7. 明确暂停状态和历史分析返回后的操作，不把暂停更新描述为释放麦克风。

首阶段不引入多 FFT、AudioWorklet、自定义 FFT、后端、音频上传或持久化录音。不因显示改版自动拆分工程或引入框架。

## 三、关键设计决策

### 1. 两条曲线的定义

| 曲线 | 项目内定义 | 首阶段建议 |
|---|---|---|
| 黄线 | 当前频谱 `spectrum` | 实线，无大面积填充 |
| 红线 | 峰值保持 `peakHold` | 保留已有 4.5 dB/s 衰减默认值，并在界面说明 |

可增加“衰减保持 / 保持至重置”选项；新增选项前先确认 UI 空间与实际需要。该默认值是本项目兼容选择，不代表 Spectroid 的行为。

```javascript
// 衰减保持：现有行为
peakHold[i] = Math.max(spectrum[i], peakHold[i] - decay);

// 保持至重置：可选行为
peakHold[i] = Math.max(spectrum[i], peakHold[i]);
```

“最近 20 秒最大值”是另一种统计，需要淘汰过期峰值，不等同于永久保持，不在首阶段默认新增。现有最大响声历史分析继续保留。

历史分析模式下，曲线必须明确标为所选统计结果；不得仍标注为实时曲线。建议继续隐藏实时峰值保持线，避免将不同时间语义混合比较。

### 2. 显示范围与输入增益分离

建议统一管理显示状态，以下参数仅为开发初值，需通过截图与测试信号调整：

```javascript
const view = {
  minHz: 20,
  maxHz: 20000,
  minDb: -120,
  maxDb: 0,
  waterfallMinDb: -110,
  waterfallMaxDb: -30
};
```

- 频率上限不得超过实际采样率的一半。
- 电平显示范围和瀑布图颜色范围独立；修改它们不得改动频谱数据和测量读数。
- 输入增益仍由 `GainNode` 调整，界面应区分“输入增益”和“颜色范围”。
- 所有范围均需校验有限值、上下限顺序及合理跨度。
- 颜色范围饱和时应有明确色标，不能使用户误以为信号超出色阶等于测量数据被截断。

### 3. 坐标及标记

- 为上下图定义相同的绘图区左右边距，所有频率坐标相对绘图区计算。
- 绘图、网格、SVG 标记及指针命中测试共用同一套变换。
- 峰值标记缩小，频率和电平文字保持可读，处理相邻峰和边缘位置重叠。
- 保留自定义测量点，并使其在两图同一频率位置对齐。
- 下方时间信息和 dB 色标分开布局，不用 dB 数字充当秒数。

### 4. 滚动历史策略

首阶段沿用“旧图下移、新行加入”的增量绘制。Spectroid 同样滚动显示，滚动机制本身不是需要修正的缺陷。

现有固定像素步长与固定 20 秒刻度只在接近预期间隔时匹配。卡顿、后台节流或尺寸变化时可能偏离。若保留秒数刻度，需按时间戳校正显示；尚未完成时间校正时，不宣称纵坐标精确代表固定 20 秒。

显示颜色范围改变时，需从 `historyFrames` 重绘已有历史，避免新旧行使用不同标尺。可保留增量绘制作为日常路径，在颜色、尺寸和后续缩放改变时触发全量重绘。

历史时间当前采用累计采集时间，暂停时不增长。首阶段保留该语义并说明；不要悄然改成墙钟时间。

## 四、代码修改映射

| 位置/函数 | 修改内容 |
|---|---|
| CSS：`.shell`、`header`、`.metrics`、`.panel`、图表高度 | 紧凑布局、深色背景、响应式图表比例 |
| `drawSpectrum()`、`drawTrace()` | 黄/红双线、关闭发光和填充、分析模式样式与图例 |
| `updatePeakHold()` | 明确衰减默认；可选保持至重置 |
| `drawFrequencyGrid()`、`drawSharedAxis()` | 主次网格、共用绘图区、动态刻度布局 |
| `frequencyToX()`、`xToFrequency()`、`dbToY()` | 统一显示范围和绘图区映射 |
| `buildTracePoints()`、`getTraceBucketInfo()` | 保留像素桶取最大值策略；范围改变后正确失效缓存 |
| `renderSpectrumOverlay()` | 紧凑文字、避让、测量线和命中区域同步 |
| `colorForLevel()` | 独立颜色范围、色阶；可预计算颜色查找表 |
| `pushWaterfallRow()`、`drawWaterfall()` | 保留增量滚动，补充基于历史快照的重绘路径 |
| `renderWaterfallOverlay()` | 时间标记与颜色标尺清楚分离 |
| `resizeCanvases()` | 同步尺寸、重绘历史、清理相关缓存 |
| 指针坐标及交互函数 | 适配新增边距，保留点击/拖动/手动输入测量点 |
| `clearHistory()`、采集及分析状态函数 | 检查清空、切换、暂停状态一致性 |

## 五、分阶段实施

### P0：开发准备

- 重新读取远端当前提交，比较本方案基线是否变化。
- 检查目标工作目录、项目指令及 Git 状态，避免覆盖用户修改。
- 使用已克隆的本地开发目录 `D:\all_z\Documents\GitHub\neon-noise`，记录实际起点提交和源文件 SHA-256。
- 在开始功能开发时，以核实后的本地 `main` 为起点创建 `modify-for-spectroid`；若同名分支已经存在，先核对其起点、改动和工作区，不覆盖或强制重建。
- 将本方案随开发分支纳入版本控制；当前方案是未跟踪文件，切换分支前后应确认内容完整。不得为清理工作区而删除方案。
- `origin` 应继续指向用户 fork。后续若需配置 `upstream`，应指向 `Arrow36/neon-noise`，不得把它替换成默认推送目标。
- 先完成第八节的来源、版权与分发设计，再开展 P1/P2；实际说明只列已实现且核验的变化。
- 核对参考截图可用性；如果没有参考图，只按本方案做风格改版，不声称逐像素复刻。

### P1：双曲线与基本布局

- 改为紧凑图表布局，保留控制入口。
- 实现黄线实时、红线保持及明确图例。
- 调整背景、网格、坐标和峰值文字。
- 完成桌面与手机尺寸检查。

### P2：瀑布图色阶与历史显示

- 增加独立颜色范围与 dB 色标。
- 保留滚动，增加从数值历史重绘的能力。
- 验证上下图对齐、颜色变化一致性和历史时间语义。
- 完成原有四种历史分析回归。

### P3：后续可选交互

- 滚轮或双指缩放、拖动平移、恢复全频段。
- 统一处理平移与测量点拖动、手机纵向页面滚动之间的冲突。
- 点击瀑布图，读取指定历史时刻和频率的数值。
- 按需求提供红线保持模式或滚动窗口最大值。

P3 不应阻塞 P1/P2 交付，也不作为首阶段已经承诺完成的功能。

### P4：后续可选分析引擎研究

仅当相同测试输入表明单 FFT 不能满足低频细节或响应速度需求时启动。研究 AudioWorklet、多分辨率分析、降采样及抗混叠、FFT 窗函数、电平一致性和计算成本。不能把多 FFT 简化成并联几个分析器后直接拼接曲线。

## 六、验收标准

### 1. 视觉与交互

- 黄/红曲线含义明确，当前线与保持线可区分。
- 同一个已知频率的曲线峰、竖线、共享刻度、瀑布图条纹对齐。
- 窄屏和桌面均无标签严重遮挡、按钮不可达或非预期横向溢出。
- 页面背景、主次网格、曲线和历史颜色有足够对比度。
- 启动、暂停、继续、清空、输入增益、测量点和历史分析均可操作。
- 切换历史分析后，标题和图例对应实际显示的数据。

### 2. 可重复信号验证

先用本地浏览器生成的测试信号接入分析节点，验证绘图与计算，再做真实麦克风验证；测试注入路径不得冒充真实采集结果。

| 信号/操作 | 检查内容 |
|---|---|
| 单频正弦，例如 100 Hz、1000 Hz | 已知频率与曲线峰、瀑布图条纹的位置关系 |
| 双频信号 | 两个峰均被画出；相近峰行为记录实际分辨率限制 |
| 阶跃降低信号幅度 | 黄线下降，红线按选定规则保持或衰减 |
| 短促信号 | 时间位置及红线保持效果，不承诺超出 FFT 时间分辨率 |
| 修改颜色范围 | 频谱数值不变，已有和新增历史颜色一致 |
| 暂停再继续 | 状态、时间标记和历史延续符合累计采集时间语义 |
| 清空历史 | 历史、分析结果和峰值保持状态按定义重置 |
| 调整窗口尺寸 | 坐标对齐、标记及历史重绘正常 |

在 48 kHz 下，32768 点 FFT 频点间距约 1.465 Hz，窗口约 0.683 秒。插值小数不是实际测量精度保证。不得拿不同设备的不同麦克风读数作为软件是否一致的唯一标准。

### 3. 性能与限制

- 首阶段保留已有绘制节流和 DPR 限制，检查连续运行满一个历史窗口及之后的表现。
- 检查历史缓存有界，颜色/尺寸变化时重绘不会持续阻塞交互。
- 记录实际测试浏览器、采样率、视口、设备和未测试项；不承诺未经实测的帧率。
- dBFS 与声压级 dB SPL 区分说明继续保留。
- 未获麦克风测试条件时完成模拟信号和静态验证，明确实际采集尚未验证。

## 七、交付与后续边界

开发完成后建议交付修改后的独立 `index.html`、完整 `LICENSE`、更新后的 `README.md`、修改说明、验证记录和前后效果截图，记录基线及产物哈希。单独交付 HTML 时还需执行第八节的内嵌许可要求。演示中使用模拟信号时应明确标注。

截至本次修订，用户 fork 已克隆到本地，本方案已移入仓库；本次仅更新开发方案。当前仍为 `main`，未在本次创建 `modify-for-spectroid`、修改应用代码或许可证、创建提交、推送、发布或部署。后续按上述分支计划实施 P0/P1/P2；推送、PR、合并、发布及部署按用户后续授权执行。创建开发分支不等于发布网站。

方案可执行的首阶段结论：保留单文件与现有 FFT，完成双曲线、紧凑图表布局、明确坐标及颜色标尺、滚动瀑布图显示改造。算法一致性和完全复刻不作为首阶段验收口径。

## 八、MIT、来源声明与社区实践

### 1. 依据与适用范围

- 用户决定继续 MIT，并采纳引用会话中保留声明、公开派生关系、明确改动、保留可追溯历史和检查交付物的建议。
- 会话参考：[检查文件保存位置](chatgpt-conversation://6aa2f3a8-5844-83ed-ad83-03f5904f7fe1)。这是讨论来源；发布时的许可依据仍是实际许可证文本。
- 本地已核查：[LICENSE](LICENSE)，原版权行为 `Copyright (c) 2026 Arrow36`，后附 MIT 许可正文和免责段。
- 许可原文参考：[Open Source Initiative：MIT License](https://opensource.org/license/mit)，2026-09-11 核对。

MIT 要求在软件副本或其实质性部分中携带相关版权与许可声明。本方案采用保留原版权行及完整 MIT 文本的落实方式。README 致谢、网页页脚署名、保留 Git 历史及向上游反馈是本项目采用的社区与可追溯实践，不应全部描述成 MIT 强制条款；也不把向原作者申请批准设为发布前置条件。

### 2. 文件级实施要求

| 对象 | 开发要求 | 验收证据 |
|---|---|---|
| `LICENSE` | 原样保留 `Copyright (c) 2026 Arrow36`、许可正文及免责段；不以用户账号替换原作者 | 与开发起点许可文本对照，确认没有遗漏 |
| 新增版权行 | 仅在用户确认实际版权主体、且存在相应原创修改时追加；默认不猜测姓名或把维护账号自动当成版权主体 | 记录主体与适用范围；未确认则不追加 |
| `README.md` | 在简介显著位置说明派生自 Arrow36/neon-noise，独立维护；列真实差异、来源基准、许可及问题反馈入口 | 文案与实际差异、提交记录一致 |
| `index.html` | 保留已有版权和许可注释；为单文件分发内嵌完整上游版权及 MIT 文本，可放 HTML 注释或可展开许可区 | 单独复制 HTML 后仍可取得完整声明；压缩不得删除它 |
| 页面页脚或关于区 | 提供简洁的上游来源、当前 fork 和许可证入口，兼顾紧凑布局 | 实際页面链接可访问；署名不冒充官方关系 |
| 变更记录 | README 或独立变更文件按新增/调整/修复记录已交付行为；保留原 Git 历史 | 起点 SHA、差异与验证结果可追溯 |

完整 MIT 文本以仓库 `LICENSE` 为来源复制，不能只写 `SPDX-License-Identifier: MIT`、一个链接或一句感谢来代替单文件中的完整声明。本方案选择内嵌，是为了适配本项目“仅复制 index.html 也可分发”的交付方式。

### 3. README 拟采用结构

下列文字是待开发完成后按事实落地的模板，不应提前宣称计划功能已实现。维护者暂按已核实的 fork 账号描述；新增版权主体另行确认。

```markdown
# NEON//NOISE（独立维护的派生版本）

本项目派生自 [Arrow36/neon-noise](https://github.com/Arrow36/neon-noise)，
保留并修改了上游实现，由 GitHub-Ericcccc 独立维护。
本版本面向紧凑双曲线及滚动历史频谱的显示需求。

## 来源与致谢

感谢 Arrow36 及上游贡献者提供的基础实现。
开发起点：[填入实际已核实的 fork 提交链接]。
上游对应基准：[仅填已验证的提交；未确认时明确待确认]。
原有版权及 MIT 许可声明完整保留。

## 与上游的主要差异

- [列出实际完成、验证过的新增或调整，不复制待办清单作为成果。]

本项目不代表上游官方发布。
本版本问题请提交至 https://github.com/GitHub-Ericcccc/neon-noise/issues 。

## 显示参考

界面呈现参考 Spectroid；本项目并非 Spectroid 官方版本。
代码来源与视觉参考分别说明，不将二者混为同一个上游。

## 许可证

本项目继续采用 MIT License，详见 [LICENSE](LICENSE)。
如引入另有许可的第三方内容，应另列其来源与适用条款。
```

现有 README 的在线链接指向上游站点。开发后应标注其为“上游演示”；只有本 fork 实际部署并核验访问成功后，才新增自己的演示地址，不能提前猜测或声称已经上线。

### 4. Spectroid 与其他资源的边界

- 沿用的代码来自 neon-noise；Spectroid 在本计划中是显示参考，不据此宣称获得其代码、图标或截图的 MIT 授权。
- 界面由现有代码修改实现，不直接打包 Spectroid 的 APK、标识、字体或截图。确需引入某项资源时先核实该项来源及许可。
- 用户提供的照片暂作分析参考，不默认提交到公开仓库或发布包。
- 若未来增加第三方库、字体或素材，按实际需要增补来源与许可清单；当前不为单一 MIT 上游预设复杂许可证目录。

### 5. 分支、PR 与交付检查

- 功能开发位于 `modify-for-spectroid`，默认拟合并目标为用户 fork 的 `main`；向 Arrow36 提交 PR 是独立、可选的回馈，不与 fork 内部合并混淆。
- 保留原提交历史，不通过重新初始化仓库或改写历史掩盖来源。
- PR 描述包含具体问题、完成后的行为、验证结果和已知限制；只有获准推送或创建 PR 后执行相应远端操作。
- 原通用发布预检组件对该公开 fork 返回“仅支持私有仓库”，后续不得把该组件误报为检查通过，也不得为通过它擅自改变仓库可见性。发布流程适配属于后续工作。

发布前验收清单：

- [ ] `LICENSE` 保留上游版权行、MIT 正文及免责段。
- [ ] 新版权行如有增加，主体已确认且没有替换上游声明。
- [ ] README 清楚说明派生、维护、实际差异、基准提交和反馈入口。
- [ ] HTML 单独分发仍包含完整版权及许可文本。
- [ ] ZIP 或其他包实际解压检查，许可文件没有被打包规则排除。
- [ ] 部署页面的来源和许可入口可用；HTML 中内嵌声明未被压缩删除。
- [ ] 当前 fork 与上游演示地址区分，未把未上线地址列为可用。
- [ ] 新增第三方资源如有，许可与来源已经逐项核对。
- [ ] 不把 Spectroid 视觉参考写成官方授权、合作或代码来源。
- [ ] 发布说明仅列已完成的改进，并保留功能和测试限制。

上述内容与 P1/P2 一并验收。本次修订将其纳入方案，实际 `LICENSE`、README 与 HTML 的改动留待开发阶段实施。

## 九、执行期追加要求（2026-09-11）

用户要求历史频谱由 20 秒扩展至 30 秒，允许图表适当拉长。本节覆盖前文所有目标窗口与验收中的 20 秒要求；上游历史事实描述仍按原样保留。开发已统一用 HISTORY_WINDOW_MS = 30000 控制缓存、历史分析与时间刻度。桌面瀑布图高度 420px，手机 390px。功能、验证报告及发布说明按 30 秒验收。

用户已一次性批准本次开发的分支、提交、推送、fork 内 PR、合并、tag、Release，并允许麦克风及浏览器验证。前文“仅更新方案”的执行状态为历史记录，不限制此次实施。
