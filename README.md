# NEON//NOISE

本项目派生自 [Arrow36/neon-noise](https://github.com/Arrow36/neon-noise)，保留并修改了上游实现，由 **GitHub-Ericcccc** 独立维护，不代表上游官方发布。显示参考 Spectroid，并非其官方版本或算法复刻。

## 本版本新增与调整

- 紧凑深色布局、黄色当前频谱、红色峰值保持及共享频率坐标。
- 显示设置可分别调整曲线范围和蓝紫瀑布图色阶，颜色变化不改变测量值；输入增益仍实际改变分析输入。
- 红线默认按 4.5 dB/s 衰减，可选择保持至重置；“重置红线”单独清除保持值。
- 历史按快照时间戳排列，保留错过采集的间隙；调色或调整尺寸后从数值重绘，缓存单行颜色减少重复计算。
- “模拟信号”生成无声的 100 Hz + 1000 Hz 输入，不访问麦克风；刷新页面切换输入来源。
- 麦克风请求 15 秒未完成时退出等待，迟到的音频流会释放。
- 完整 MIT 文本内嵌于 HTML，单文件分发也携带许可。

当前未新增多 FFT、双指缩放或历史时刻选读。点击瀑布图仍只选择频率。

**暂停仅冻结图表与历史，麦克风仍可能占用；关闭页面释放。历史时间按累计采集时长计，暂停时间不计入。** “返回实时频谱”只切换视图，再点击“继续采集”恢复更新。历史分析结果使用青色线，实时模式使用黄红线。

一个直接运行在浏览器中的实时噪音频谱分析仪，用于观察环境噪音、定位稳定频率峰值，并回看最近 30 秒的频谱变化。

## 在线使用

[上游在线演示](https://arrow36.github.io/neon-noise/)展示上游版本，并非本 fork。本 fork 尚未公布经验证的在线部署地址；本地运行见下文。

首次使用时，请允许浏览器访问麦克风。建议使用最新版 Chrome 或 Edge；部分内置浏览器可能不会开放麦克风接口。

## 主要功能

- 32768 点 FFT 实时频谱分析
- 对数频率坐标与峰值保持
- 最近 30 秒的历史频谱瀑布图
- 平均功率、中位数、最大响声及噪声突出度分析
- 可点击、拖动或手动输入频率的自定义测量点
- 输入增益调节、暂停与继续采集
- 针对手机和低性能设备降低绘制负载

## 本地运行

麦克风接口通常要求安全上下文。不要直接用 `file://` 打开文件，请在项目目录启动本地 HTTP 服务，例如：

```bash
python -m http.server 8000
```

然后访问 <http://localhost:8000/>。

## 使用方法

1. 点击“启动麦克风”并允许麦克风权限。
2. 在当前频谱中观察主要峰值，在历史频谱中查看它们是否持续出现。
3. 点击任一图表可创建测量点；拖动测量点可改变频率，再次点击标签可手动输入频率。
4. 点击“分析历史频谱”，从已有的最近 30 秒数据生成分析结果；不必等待满 30 秒。
5. 使用分析方式切换总体能量、稳定底噪、短促异响和持续噪声特征。

## 分析方式

- **平均功率频谱（总体能量）**：先在线性功率域求平均，适合观察一段时间内的总体声能。
- **中位数频谱（稳定底噪）**：降低偶发声音的影响，更适合寻找持续存在的背景噪音。
- **最大响声频谱（短促异响）**：保留每个频率在历史窗口内的最高值，突出短暂事件。
- **噪声频谱（噪声突出度＋出现率）**：综合频率相对周边的突出程度和历史出现频率，帮助寻找持续窄带噪音。

## 读数说明

- 频率单位为 Hz。
- 电平单位为 dBFS，是相对于数字满刻度的电平，并非经过声压校准的 dB SPL。
- 频谱结果会受到麦克风频响、自动增益、降噪处理、设备摆放和房间反射影响。
- 本工具适合定位和比较噪音频率，不替代经过校准的专业声级计或测量麦克风。

## 隐私

音频只在当前浏览器中通过 Web Audio API 实时处理。此项目不包含服务器端音频上传或存储功能，刷新或关闭页面后历史数据会消失。

## 技术结构

项目采用无依赖的单文件实现：页面、样式与 JavaScript 分析逻辑都位于 `index.html`，无需安装或构建。

## 许可证

本项目基于 [MIT License](LICENSE) 开源。

原版权行 `Copyright (c) 2026 Arrow36` 及完整 MIT 许可与免责段均已保留。未添加未经确认的新版权主体，未分发 Spectroid 的程序、图片或标识。

感谢 Arrow36 及上游贡献者提供基础实现。开发起点为本 fork 的提交 [b38c4f9](https://github.com/GitHub-Ericcccc/neon-noise/commit/b38c4f9d7e25a8359fb50f81ce72ab50e3295d4d)，已验证它是上游 `0f96ec683b72cf1f4b2dde26d24ec9ce3ee2fefc` 的祖先；两者仅相差一行上游站点验证标签，本版本不引入该标签。

问题请提交至[本仓库 Issues](https://github.com/GitHub-Ericcccc/neon-noise/issues)。

## 验证与限制

运行 `node tests/analysis.cjs`，无需安装依赖。验证记录见 [开发验证报告](docs/validation.md)，版本变化见 [CHANGELOG](CHANGELOG.md)。

本机内置浏览器已验证麦克风启动和频谱变化；尚未完成多设备兼容性或声学标定，模拟信号测试不替代这些测试。48 kHz / 32768 点的频点间距约 1.465 Hz，窗口约 0.683 秒；插值小数不代表相同测量精度。
